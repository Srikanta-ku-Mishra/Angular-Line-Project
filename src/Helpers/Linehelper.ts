import { LinePoints } from 'src/Constants/points/points';
import * as THREE from 'three'

export class Linehelper {
    constructor() {
        // practicing  
        // const points: THREE.Vector3 = LinePoints;
        const material = new THREE.LineBasicMaterial({
            color: 0xffffff
        });
        // // const geometry1 = new THREE.BufferGeometry().setFromPoints(this.points);
        // const line = new THREE.Line(geometry1, material);
        // return line;


    }
}